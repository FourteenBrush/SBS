package me.fourteendoggo.sbs.argument.address;

// TODO: why is this even a class on its own?
public class Register extends Address {

    public Register(int registerOffset) {
        super(registerOffset);
    }
}
